import { type ILayoutConfig } from '@/providers';

// Defining the layout configuration specific to Demo3 layout
const AppLayout3Config: ILayoutConfig = {
  name: 'applayout3-layout', // Unique name identifier for this layout
  options: {}
};

export { AppLayout3Config };
